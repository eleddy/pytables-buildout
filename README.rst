Introduction
============
Working on a buildout for pytables. Current hold up is that numexpr relies
on numpy already being installed to run setup. distutil mania. 

To run to builout::

  python bootstrap.py
  ./bin/buildout

